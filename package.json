{
  "name": "eucalypt",
  "version": "0.0.1",
  "description": "Reagent compatibale-ish Squint-cljs library with no React. Builds tiny frontends.",
  "type": "module",
  "scripts": {
    "build": "npm run demo:build",
    "clean": "find ./ -name '*.mjs' | xargs rm",
    "dev": "npm run demo:dev",
    "test": "npm run test:compile && vitest run --config test/vitest.config.js",
    "demo:build": "cd demo && squint compile && cd .. && vite build demo",
    "demo:watch": "cd demo && squint watch",
    "demo:dev": "concurrently \"npm run demo:watch\" \"vite demo --port 8000\"",
    "test:compile": "cd test && squint compile && cd .."
  },
  "devDependencies": {
    "concurrently": "8.2.2",
    "es-toolkit": "1.39.10",
    "happy-dom": "14.12.3",
    "squint-cljs": "latest",
    "vite": "7.1.2",
    "vite-plugin-singlefile": "2.3.0",
    "vitest": "1.6.0"
  }
}
