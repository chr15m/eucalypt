{
  "name": "eucalypt",
  "version": "0.0.10",
  "exports": "./eucalypt.mjs",
  "description": "Reagent compatible-ish Squint-cljs library with no React. Builds tiny frontends.",
  "type": "module",
  "scripts": {
    "build": "npm run demo:build; npm run dist:build",
    "clean": "find . -name '*.mjs' -not -path './node_modules/*' -delete",
    "analyze": "ANALYZE=1 npm run build",
    "watch": "npm run dev",
    "dev": "npm run demo:dev",
    "test": "npm run test:compile && vitest run --config src/test/vitest.config.js",
    "test:ci": "npm run test:compile && vitest run --reporter=default --reporter=json --outputFile=./test-results.json --config src/test/vitest.config.js",
    "demo:build": "cd src/demo && squint compile && vite build && SRC=small.html vite build && SRC=games.html vite build",
    "dist:build": "cd src && squint compile",
    "demo:watch": "cd src/demo && squint watch",
    "demo:dev": "concurrently \"npm run demo:watch\" \"vite src/demo --port 8000\"",
    "test:compile": "cd src/test && squint compile"
  },
  "devDependencies": {
    "concurrently": "8.2.2",
    "happy-dom": "14.12.3",
    "squint-cljs": "0.8.156",
    "vite": "7.1.2",
    "vite-bundle-analyzer": "^1.2.3",
    "vite-plugin-singlefile": "2.3.0",
    "vitest": "3.2.4"
  },
  "files": [
    "README.md",
    "eucalypt.mjs"
  ]
}
